{
  "name": "logicgate",
  "version": "1.0.0",
  "description": "my js learning notes",
  "main": "index.js",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.test.ts",
    "coverage": "nyc -r lcov -e .ts -x \"*.test.ts\" npm run test",
    "test-single": "mocha -r ts-node/register",
    "test-watch": "mocha -r ts-node/register -R list -w tests/**/*.test.ts"
  },

  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/chai": "^4.2.4",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.12.5",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "nyc": "^14.1.1",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "@types/mathjs": "^6.0.4"
  }
}
